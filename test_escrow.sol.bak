pragma solidity >=0.8.0;

import "truffle/Assert.sol";
import "truffle/DeployedAddresses.sol";
import "../contracts/TinanceEscrow.sol";
import "../contracts/USDTToken.sol";

contract TestMetaCoin {

  function testInitialBalanceUsingDeployedContract() public {
    TinanceEscrow escrowT = TinanceEscrow(DeployedAddresses.USDTToken()); 
       
    Assert.equal( escrowT.getVersion(), "Escrow V1.198", "version wrong");
    
  }

  function testInitialBalanceWithNewMetaCoin() public {
   // USDTToken meta = new USDTToken();

   // uint expected = 500000;

  //  Assert.equal(meta.balanceOf(tx.origin), expected, "Owner should have 500000 initially");
  }
}
